namespace Draughts {
    /**
     * Interaction mode enumeration (mock)
     */
    public enum InteractionMode {
        CLICK_TO_SELECT,
        DRAG_AND_DROP
    }

    /**
     * Announcement detail level (mock)
     */
    public enum AnnouncementLevel {
        OFF,
        MOVE_ONLY,
        MOVE_AND_CAPTURE,
        FULL_DESCRIPTION
    }
}
